# i3status configuration file.

general {
    interval = 1
    colors = true
    color_good="#c0c0c0"
    color_degraded="#87afaf"
    color_bad="#d7875f"
}

order += "disk /"
order += "wireless wlan0"
order += "cpu_usage"
order += "memory"
order += "tztime local"

tztime local {
    format = " %time "
    format_time = "%H:%M %Z"
}

disk "/" {
    format =" disk free: %avail "
}

memory {
    format = " RAM in use: %used "

}

wireless wlan0 {
    format_up=" 📶 %essid: %ip "
    format_down=" wifi: down "
    format_quality=" %03d%s "
}

cpu_usage {
    format = " CPU used: %usage "
}


